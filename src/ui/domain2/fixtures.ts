/**
 * Five units, one per group, for the unit tests and the kit page. Never imported by the app: the
 * real units come from `src/data`.
 */
export { ABYSS_MARAUDER, ARCHER, CATAPULT, FIRE_ELEMENTAL, SAMPLE_UNITS, SPEARMAN } from '../domain/fixtures';
