/**
 * March section contract (M-08). The shell (M-03) renders these in the March pane header, the phone
 * bottom app bar and the recap sheet; the March worker replaces the stubs with the real components.
 * Names and props are fixed here; do not rename.
 */
export { MarchSection, MarchQuickSummary, MarchRecap, MarchGenerateButton } from './stubs';
export type { MarchQuickSummaryProps, MarchRecapProps, MarchGenerateButtonProps } from './stubs';
