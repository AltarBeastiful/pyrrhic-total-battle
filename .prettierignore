node_modules/
dist/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml
LICENSE

# Scratch git worktrees Claude Code parks inside the repo: copies of the tree, not sources.
.claude/

# Hand-written prose, captured TotalStack output and in-game battle-report
# transcripts: line breaks there are meaningful, Prettier must not rewrap them.
docs/

# Captured TotalStack output and in-game battle-report transcripts used as engine regression
# fixtures: verbatim copies of docs/research/fixtures, Prettier must not rewrap them.
tests/fixtures/

# Generated experiment outputs (investigation 0015): tables written by the scripts, not hand-formatted.
tools/theorycraft/out/

# AssemblyScript sources: decorators on top-level functions (`@inline`) are not TypeScript Prettier parses.
kernel/assembly/
