node_modules/
dist/
coverage/
playwright-report/
test-results/
*.local
.DS_Store
.vite/
.claude/worktrees/
.claude/settings.local.json
docs/design-canvas/*.html
!docs/design-canvas/*.dc.html

# The benchmark writes its run here and promotes it only when whole (S-121b).
tools/theorycraft/out/benchmark-run.json
tools/theorycraft/out/benchmark-run.md

# The AssemblyScript kernel, built on demand (`pnpm kernel:build`, or by the parity test when stale).
kernel/build/
# The same run with the plan kernel set (the `kernel` vitest project); never committed.
tools/theorycraft/out/benchmark-run.kernel.json
tools/theorycraft/out/benchmark-run.kernel.md
tools/theorycraft/out/benchmark-latest.kernel.json
tools/theorycraft/out/benchmark-latest.kernel.md
